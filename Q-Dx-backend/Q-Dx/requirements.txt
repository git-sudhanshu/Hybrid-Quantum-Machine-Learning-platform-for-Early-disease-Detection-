# Q-Dx :: quantum component dependencies
# Verified working together on Python 3.11.15 (tested 2026-09-12).
# Lower bounds, not hard pins -- newer patch releases are fine.

pennylane>=0.41,<0.46      # quantum ML framework + default.qubit simulator
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4          # metrics, stratified splitting
matplotlib>=3.8            # circuit diagrams (qml.draw_mpl)
joblib>=1.3                # model persistence

# NOTE: PennyLane pulls in autograd, networkx, rustworkx and scipy itself.
# Do NOT add qiskit -- we have no technical need for it and it drags in a
# large dependency tree that has historically conflicted with PennyLane.
