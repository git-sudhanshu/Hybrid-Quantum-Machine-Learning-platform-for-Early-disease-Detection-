"""Q-Dx source package.

Marks `src/` as an importable Python package so that
`from src.quantum_model import train_vqc` works when you run scripts from
the Q-Dx project root. This file must exist -- without it Python raises
ModuleNotFoundError: No module named 'src'.
"""
